import { Text, View } from "react-native";

const HomeScreen = () => {
  return (
    <View className="flex justify-center items-center">
      <Text className="text-2xl text-red-400">Home Screen</Text>
    </View>
  );
};

export default HomeScreen;
